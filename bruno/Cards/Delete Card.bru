meta {
  name: Delete Card
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/cards/:id
  body: none
  auth: bearer
}

params:query {
  ~lang: es
}

headers {
  ~Accept-Language: en
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: 2
}
