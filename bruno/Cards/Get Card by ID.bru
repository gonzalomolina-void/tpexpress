meta {
  name: Get Card by ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/cards/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  ~lang: es
}

params:path {
  id: 1
}

headers {
  ~Accept-Language: en
}
