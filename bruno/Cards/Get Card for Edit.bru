meta {
  name: Get Card for Edit
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/cards/:id/edit
  body: none
  auth: bearer
}

params:query {
  ~lang: es
}

headers {
  ~Accept-Language: en
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: 1
}
