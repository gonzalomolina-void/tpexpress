meta {
  name: Add Favorite
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/favorites
  body: json
  auth: bearer
}

params:query {
  ~lang: es
}

headers {
  ~Accept-Language: en
}

body:json {
  {
    "cardId": 1
  }
}

auth:bearer {
  token: {{token}}
}
