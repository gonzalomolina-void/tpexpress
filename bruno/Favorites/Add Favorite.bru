meta {
  name: Add Favorite
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/favorites
  body: json
  auth: bearer
}

body:json {
  {
    "cardId": 1
  }
}

auth:bearer {
  token: {{userToken}}
}
