meta {
  name: Get Favorites
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/favorites
  body: none
  auth: bearer
}

params:query {
  ~lang: es
}

headers {
  ~Accept-Language: es
}

auth:bearer {
  token: {{userToken}}
}
